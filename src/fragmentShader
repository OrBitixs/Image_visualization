#version 450 core
out vec4 FragColor;
in vec4 vertexColor;
uniform vec4 blendColor;
void main()
{
   FragColor = vec4(vertexColor.x*blendColor.x, vertexColor.y*blendColor.y, vertexColor.z*blendColor.z, vertexColor.w*blendColor.w);
}
